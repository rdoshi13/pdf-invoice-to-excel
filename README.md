# pdf-invoice-to-excel

A local Python command-line tool that converts Walmart invoice PDFs into an Excel workbook for grocery cost splitting.

Drop Walmart order/invoice PDFs into an input folder, run one command, and the app creates a workbook where each invoice becomes its own worksheet. The generated sheets include item costs, editable participant flags, per-person formulas, and final totals.

## What It Does

- Reads every `.pdf` file from an input folder.
- Extracts text from Walmart invoice/order PDFs.
- Parses order date, order number, item names, item costs, tax, and total.
- Creates or updates a single `.xlsx` workbook.
- Adds one worksheet per invoice, named by order date, such as `1 March` or `16 June`.
- Keeps duplicate sheet names safe by appending a number, such as `16 June 2`.
- Adds formulas so the split totals update automatically when participant flags are edited.

## Current Scope

This tool is currently built for Walmart PDFs that contain selectable/extractable text. Scanned image PDFs are not supported yet.

The workbook layout is customized for a fixed grocery split sheet with these participants:

- Rakshit
- Ansh
- Rishabh
- Varun
- Anuj

Only the participant flag columns are meant to be edited manually after generation. The formula columns and total row are generated by the program.

## Requirements

- Python 3.11 or newer recommended
- `pdfplumber`
- `openpyxl`
- `pytest` for running tests

Install dependencies:

```bash
pip install -r requirements.txt
```

## Usage

1. Put Walmart invoice PDFs into the `input/` folder.
2. Run:

```bash
python src/main.py --input input --output output/walmart_orders.xlsx
```

3. Open `output/walmart_orders.xlsx` in Excel, Apple Numbers, or Google Sheets.
4. In each worksheet, enter `1` or `0` in the participant flag columns to mark who shared each item.

Example output:

```text
Found 6 PDF files.
Parsed 6 invoices.
Workbook: output/walmart_orders.xlsx
Added sheets: 1 March, 31 March, 1 May, 16 May, 25 May, 16 June
Failed PDFs: 0
Done.
```

## Workbook Layout

Each invoice gets one worksheet.

Rows:

```text
Row 1: merged title row, e.g. Walmart 25 May
Row 2: column headers
Row 3+: invoice item rows
Final row: Total
```

Columns:

```text
A: Items
B: Cost
C: Rakshit
D: Ansh
E: Rishabh
F: Varun
G: Anuj
H: Involved
I: Per Person
J: Rakshit
K: Ansh
L: Rishabh
M: Varun
N: Anuj
```

The app also adds `Walmart Tax` as a split-able row when tax is present in the invoice.

Generated formulas include:

```text
H3 = SUM(C3:G3)
I3 = IF(H3=0,0,B3/H3)
J3 = IF(C3=1,I3,0)
K3 = IF(D3=1,I3,0)
L3 = IF(E3=1,I3,0)
M3 = IF(F3=1,I3,0)
N3 = IF(G3=1,I3,0)
```

The final row sums total item cost and each participant's owed amount.

## Supported Input

The parser expects Walmart invoice text similar to:

```text
Invoice
May 12, 2026 order
Order# 2000147-51516982
Fresh Roma Tomato, Each Weight-adjusted Qty 12 $6.20
Coca-Cola Zero Sugar Soda Pop Bottle, 2 Liters Shopped Qty 2 $5.94
Tax $0.93
Total $37.49
```

Known item statuses include:

- `Weight-adjusted`
- `Shopped`
- `Return complete`
- `You're all set! No need to return this item`

If a PDF cannot be parsed, the app skips it, continues processing the rest, and writes debug text under `output/debug/` when extracted text is available.

## Running Tests

```bash
pytest
```

The tests cover Walmart parsing, participant date rules, workbook formulas/layout, duplicate sheet names, and CLI orchestration.

## Project Structure

```text
src/
  main.py               CLI entrypoint and orchestration
  models.py             Shared dataclasses
  pdf_reader.py         PDF text extraction
  walmart_parser.py     Walmart invoice parser
  participant_rules.py  Date-aware participant rules
  excel_writer.py       Workbook generation
tests/
  test_*.py
```

## Limitations

- No OCR for scanned PDFs.
- No web UI.
- No Google Sheets API integration.
- No cloud upload.
- No AI/LLM parsing.
- The split-sheet layout and participants are currently hardcoded.

## Future Ideas

- Add a summary sheet across all orders.
- Detect duplicate PDFs by order number.
- Add monthly spending summaries.
- Make participants configurable.
- Add OCR fallback for scanned invoices.
- Add Google Sheets export.
